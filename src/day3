import pandas as pd

file_path = "../data/jobs-usa.csv"
df = pd.read_csv(file_path)
print(df.info())
print(df.describe())
print(df.isnull().sum())
df["salary_clean"] = df.to_numeric.apply["salary"]
df["salary_clean"] = dropna.df["salary_clean"]
salary = salary.replace("$", "").replace(",", "")

df["skill_count"] = df[skills].sum(axis=1)
df["skill_count"] = df[skills].sum(axis=1)
#skills more salary more 
#outlinrs 
#1.remove outliers  creat skill_count 